# Extra packages for Airflow DAGs (base image already includes Airflow 2.8.1 + SQLAlchemy 1.4.x)
#
# Keep numpy 1.x — the base image ships pandas 2.1.x compiled against numpy 1.x.
# Unpinned scikit-learn/dbt previously upgraded numpy to 2.x and broke pandas imports.
numpy>=1.24.4,<2
scipy>=1.10,<1.14
scikit-learn>=1.3,<1.6
joblib>=1.3
pandas>=2.1,<2.3
pyarrow>=14,<18
# Match repo root (requirements.txt) so joblib bundles load in Airflow and locally
xgboost==3.2.0
dbt-postgres>=1.8,<1.9
