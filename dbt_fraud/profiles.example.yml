# Copy to dbt_fraud/profiles.yml (used by `dbt run --profiles-dir .` and the dashboard refresh button)
fraud_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5433
      user: fraud
      password: fraud
      dbname: fraud_db
      schema: public
      threads: 4
