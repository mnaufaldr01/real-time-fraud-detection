# Copy to ~/.dbt/profiles.yml
fraud_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5433
      user: fraud
      password: fraud
      dbname: fraud_db
      schema: public
      threads: 4
